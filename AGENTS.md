<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
<!-- SPECKIT END -->

Before making changes, read `.specify/memory/constitution.md`; it defines the
project rules, data boundaries, and module contracts.

Feature specifications live in `specs/`. Start with
`specs/001-subsurface-surrogate/spec.md` unless the user names another feature.

Do not copy the raw 177-case MRST training corpus from `boreyes2026` into this
repo. Only use derived artifacts needed to run, validate, or write up the
framework, and keep large binaries under the ignored data/model folders.
