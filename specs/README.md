# Feature Specs

This directory is managed manually for this solo Spec Kit project. The feature
numbering is stable and matches the intended implementation order.

Start planning with `001-subsurface-surrogate`; downstream modules depend on its
trained-range flags, production deltas, inventory series, and checkpoint metadata.
